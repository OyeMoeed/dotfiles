import createStyleSheet from '@app/styles/scaled-sheet.styles';

const permissionSheetStyles = () =>
  createStyleSheet({
    title: { textAlign: 'center' },
  });

export default permissionSheetStyles;

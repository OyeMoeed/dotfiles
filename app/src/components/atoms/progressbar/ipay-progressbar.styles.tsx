import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 5,
    backgroundColor: '#e0e0e0',
    borderRadius: 5,
    overflow: 'hidden',
    width: '70%'
  },
  progress: {
    height: '100%',
    borderRadius: 5
  }
});

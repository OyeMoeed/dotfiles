import createStyleSheet from '@app/styles/scaled-sheet.styles';

const walletTransferStyles = (theme: any) =>
  createStyleSheet({
    container: {
      flex: 1,
    },
  });

export default walletTransferStyles;

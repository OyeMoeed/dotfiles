/**
 * Exports a collection of theme files for easy import.
 */
import colors from './colors.const';
import layout from './layout.const';
import theme from './theming/theme';

export default { colors, theme, layout };
